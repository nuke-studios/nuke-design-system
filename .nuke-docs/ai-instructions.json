{
  "_purpose": "AI behavior guidelines - how AI should work on this project",
  "_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "_purpose": {
        "description": "Purpose of this file",
        "type": "string"
      },
      "items": {
        "items": {
          "properties": {
            "description": {
              "type": "string"
            },
            "id": {
              "pattern": "^instruction-[0-9]+$",
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "title",
            "description"
          ],
          "type": "object"
        },
        "type": "array"
      }
    },
    "required": [
      "_purpose",
      "items"
    ],
    "type": "object"
  },
  "items": [
    {
      "id": "instruction-1",
      "title": "Component Structure",
      "description": "Each component has a single [name].css file with: reset styles, base styles, and style variants (nuke-style='1/2/3'). Use design tokens directly from style.css - no component-specific variables."
    },
    {
      "id": "instruction-2",
      "title": "Web Components",
      "description": "Web components use Lit. They enhance native elements, not replace them. CSS works without JS, components add interactivity."
    },
    {
      "id": "instruction-3",
      "title": "Token Naming",
      "description": "Always use 1-2-3 suffix pattern for tokens (e.g., --border-radius-1, --z-index-2, --width-3). Never use semantic names like --button-bg or --input-border."
    }
  ]
}