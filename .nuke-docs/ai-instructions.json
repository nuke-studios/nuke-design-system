{
  "_purpose": "AI behavior guidelines - how AI should work on this project",
  "_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "_purpose": {
        "description": "Purpose of this file",
        "type": "string"
      },
      "items": {
        "items": {
          "properties": {
            "description": {
              "type": "string"
            },
            "id": {
              "pattern": "^instruction-[0-9]+$",
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "title",
            "description"
          ],
          "type": "object"
        },
        "type": "array"
      }
    },
    "required": [
      "_purpose",
      "items"
    ],
    "type": "object"
  },
  "items": [
    {
      "id": "instruction-1",
      "title": "Component Structure",
      "description": "Each component has a single [name].scss file with: reset styles and base styles using design tokens directly. Use tokens from tokens.scss - no component-specific variables. All styling controlled via token references (--border-component, --background-component, etc.)."
    },
    {
      "id": "instruction-2",
      "title": "Web Components",
      "description": "Web components use Lit. They enhance native elements, not replace them. CSS works without JS, components add interactivity."
    },
    {
      "id": "instruction-3",
      "title": "Token Naming",
      "description": "Use tree-level suffixes for cascading tokens: --gap-shell/primitive/component, --radius-shell/primitive/component, --border-shell/primitive/component, --background-shell/primitive/component. Use 1-2-3 suffixes for scale tokens: --space-1/2/3, --width-1/2/3, --z-index-1/2/3. Never use semantic names like --button-bg."
    },
    {
      "id": "instruction-4",
      "title": "Prefer grid over flex",
      "description": "Always prefer CSS Grid over Flexbox for layouts. Grid is more powerful and predictable for 2D layouts."
    },
    {
      "id": "instruction-5",
      "title": "No margin on elements",
      "description": "Never add margin to component elements. Use gap on parent containers (grid/flex) for spacing instead."
    },
    {
      "id": "instruction-6",
      "title": "Workspace Structure",
      "description": "Project has two packages: lib/ (design system - CSS tokens, components, web components) and frontend/ (Svelte 5 docs app). Frontend imports lib via relative paths (../../lib/dist/). Icons are copied to frontend/public/icons/ for Vite serving."
    },
    {
      "id": "instruction-7",
      "title": "Frontend Development",
      "description": "Run 'npm run dev' in frontend/ folder. Uses Vite + Svelte 5 with runes. Import lib CSS via @import '../../lib/dist/core.css' and JS via import '../../lib/dist/core.js'. Configure nuke-icon basePath to '/icons'."
    },
    {
      "id": "instruction-8",
      "title": "Token Overrides",
      "description": "Frontend can override lib tokens in style.css. Override tokens at :root for global changes, or on any container for scoped changes. Both dark-theme and light-theme classes supported."
    },
    {
      "id": "instruction-9",
      "title": "Token Usage by Layer",
      "description": "Use tokens matching folder level: 1-app-shell uses --*-shell tokens, 2-primitives uses --*-primitive tokens, 3-components uses --*-component tokens. This applies to gap, radius, border, and background token families."
    },
    {
      "id": "instruction-10",
      "title": "No Hardcoded Borders",
      "description": "Never use 'border: 1px solid var(--border-color-1)' directly. Always use token references: --border-primitive for 2-primitives, --border-component for 3-components. This enables style mode switching via token overrides."
    }
  ]
}
